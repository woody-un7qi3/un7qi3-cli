# uq logs (EB 실시간 로그 스트리밍) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `uq logs <repo>` 가 등록된 EB 레포의 국가/환경을 선택해 전 인스턴스의 `web.stdout.log` 를 `eb ssh` 로 실시간 멀티플렉스 스트리밍한다.

**Architecture:** 설정은 `repos.yml` 의 `logs:` allowlist(국가→app+region). 환경/인스턴스는 `aws` 로 실시간 발견. `internal/logs` 의 `Source` 인터페이스(EB 드라이버) 가 발견·tail 명령을 캡슐화하고, 명령 레이어가 국가/환경 선택(huh TUI + 위치인자 필터)·merged/split 출력을 오케스트레이션한다.

**Tech Stack:** Go, cobra, gopkg.in/yaml.v3, huh(TUI), `aws`/`eb` CLI, `internal/exec` 래퍼, `internal/run`(터미널 분할).

## Global Constraints

- 커밋 메시지는 **한글**, AI 표기 금지.
- 코드 수정 후 `make install` (PostToolUse 훅이 자동 실행하나, 검증 시 직접 빌드도 함).
- 로그 경로 기본값: `/var/log/web.stdout.log` (상수 `repocfg.DefaultLogPath`).
- `uq` 는 보안그룹을 변경하지 않는다 — eb ssh `--force` 절대 사용 금지.
- 에러 코드: aws 미인증 exit 4 / 사용법 exit 2 / 런타임 exit 1.
- logs split 은 **cmux·iterm2 만**; 그 외는 merged 폴백.
- 외부 명령(aws/eb)은 테스트에서 주입 가능한 runner 로 호출 — 단위테스트가 네트워크에 의존하지 않게.

---

## File Structure

```
internal/repocfg/repocfg.go        [수정] LogsConfig/CountryTarget + Config.Logs + LogsFor + DefaultLogPath
internal/repocfg/repos.yml         [수정] logs: 블록 (forceteller-api)
internal/repocfg/repocfg_test.go   [수정] logs 파싱·기본값 테스트
internal/logs/source.go            [신규] Source 인터페이스 + Target/Instance
internal/logs/eb.go                [신규] EB 드라이버 (Environments/Instances/TailArgs, 주입 runner)
internal/logs/eb_test.go           [신규] TailArgs·파싱 단위테스트
internal/logs/selectlogic.go       [신규] 위치인자→국가/환경필터 순수 해석 + 필터 매칭
internal/logs/selectlogic_test.go  [신규] 해석/매칭 단위테스트
internal/logs/mux.go               [신규] merged 출력(범례·prefix·grep) + 스트리밍
internal/logs/mux_test.go          [신규] 범례·prefix·grep 순수 헬퍼 테스트
internal/run/panel.go              [신규] split.go 에서 추출한 공유 패널(Panel/OpenPanel)
internal/cmd/run/split.go          [수정] run.OpenPanel 사용(동작 불변)
internal/cmd/logs/logs.go          [수정] stub → 와이어링·플래그·오케스트레이션
internal/cmd/doctor/doctor.go      [수정] eb 도구 추적 추가
```

---

## Task 1: repocfg — logs allowlist 파싱

**Files:**
- Modify: `internal/repocfg/repocfg.go`
- Modify: `internal/repocfg/repos.yml`
- Test: `internal/repocfg/repocfg_test.go`

**Interfaces:**
- Produces:
  - `repocfg.DefaultLogPath = "/var/log/web.stdout.log"`
  - `type CountryTarget struct { App string; Region string }`
  - `type LogsConfig struct { Path string; Countries map[string]CountryTarget }`
  - `(LogsConfig).PathOrDefault() string`
  - `(*Config).LogsFor(repo string) (LogsConfig, bool)`
  - `Config.Logs map[string]LogsConfig`

- [ ] **Step 1: Write the failing test**

`internal/repocfg/repocfg_test.go` 에 추가:

```go
func TestLogsForReturnsConfiguredRepo(t *testing.T) {
	cfg, err := Load()
	if err != nil {
		t.Fatalf("load: %v", err)
	}
	lc, ok := cfg.LogsFor("forceteller-api")
	if !ok {
		t.Fatal("forceteller-api 가 logs 에 등록돼 있어야 함")
	}
	if lc.PathOrDefault() != DefaultLogPath {
		t.Errorf("기본 경로 기대 %q, 실제 %q", DefaultLogPath, lc.PathOrDefault())
	}
	kr, ok := lc.Countries["kr"]
	if !ok {
		t.Fatal("kr 국가가 있어야 함")
	}
	if kr.App != "kr-forceteller-api" || kr.Region != "ap-northeast-2" {
		t.Errorf("kr 매핑 틀림: %+v", kr)
	}
}

func TestLogsForUnknownRepo(t *testing.T) {
	cfg, _ := Load()
	if _, ok := cfg.LogsFor("forceteller-app"); ok {
		t.Error("logs 미등록 레포는 false 여야 함")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/repocfg/ -run TestLogsFor -v`
Expected: 컴파일 실패(`LogsFor`/`DefaultLogPath` 미정의) 또는 FAIL.

- [ ] **Step 3: repos.yml 에 logs 블록 추가**

`internal/repocfg/repos.yml` 끝에 추가:

```yaml

# logs: `uq logs <repo>` 대상 allowlist. 국가별 EB application + region 을 선언한다.
#       환경(beta/prod)·인스턴스는 aws 로 실시간 발견하므로 적지 않는다.
logs:
  forceteller-api:
    path: /var/log/web.stdout.log
    countries:
      kr: { app: kr-forceteller-api, region: ap-northeast-2 }
      en: { app: en-forceteller-api, region: ap-southeast-1 }
      jp: { app: jp-forceteller-api, region: ap-northeast-1 }
```

- [ ] **Step 4: repocfg.go 에 타입·접근자 추가**

`internal/repocfg/repocfg.go` 의 `Config` 구조체에 필드 추가:

```go
type Config struct {
	Repos    map[string][]string  `yaml:"repos"`
	Defaults []string             `yaml:"defaults"`
	Runs     map[string]RepoRuns  `yaml:"runs"`
	Logs     map[string]LogsConfig `yaml:"logs"`
}
```

파일 하단(다른 타입 정의 부근)에 추가:

```go
// DefaultLogPath 는 logs.path 미지정 시 사용할 EB 인스턴스 로그 경로.
const DefaultLogPath = "/var/log/web.stdout.log"

// LogsConfig 는 한 레포의 uq logs 설정. Countries 는 국가코드→(app, region).
type LogsConfig struct {
	Path      string                   `yaml:"path"`
	Countries map[string]CountryTarget `yaml:"countries"`
}

// CountryTarget 은 한 국가의 EB application 이름과 리전.
type CountryTarget struct {
	App    string `yaml:"app"`
	Region string `yaml:"region"`
}

// PathOrDefault 는 설정된 로그 경로, 없으면 DefaultLogPath.
func (l LogsConfig) PathOrDefault() string {
	if l.Path != "" {
		return l.Path
	}
	return DefaultLogPath
}

// LogsFor 는 레포의 logs 설정을 반환한다. 미등록이면 ok=false.
func (c *Config) LogsFor(repo string) (LogsConfig, bool) {
	lc, ok := c.Logs[repo]
	return lc, ok
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `go test ./internal/repocfg/ -run TestLogsFor -v`
Expected: PASS (2 테스트)

- [ ] **Step 6: Commit**

```bash
git add internal/repocfg/repocfg.go internal/repocfg/repos.yml internal/repocfg/repocfg_test.go
git commit -m "feat(repocfg): logs allowlist(국가→app/region) 파싱 추가"
```

---

## Task 2: logs Source 인터페이스 + EB 드라이버

**Files:**
- Create: `internal/logs/source.go`
- Create: `internal/logs/eb.go`
- Test: `internal/logs/eb_test.go`

**Interfaces:**
- Consumes: 없음 (repocfg 와 독립; Target 은 호출자가 repocfg 에서 구성).
- Produces:
  - `type Target struct { Country, App, Region string }`
  - `type Instance struct { ID string; Num int; Label string }`
  - `type Source interface { Environments(Target)([]string,error); Instances(Target,string)([]Instance,error); TailArgs(Target,string,Instance,bool,int)[]string }`
  - `func NewEBSource(path string) *EBSource` (기본 runner = uqexec.Run)
  - `EBSource.runner func(string,...string)([]byte,error)` (테스트 주입용 노출 필드)

- [ ] **Step 1: source.go 작성 (타입·인터페이스, 테스트 전 골격)**

`internal/logs/source.go`:

```go
// Package logs streams EB instance logs for `uq logs <repo>`.
package logs

// Target 은 한 국가의 EB application 과 region.
type Target struct {
	Country string
	App     string
	Region  string
}

// Instance 는 EB 환경의 한 인스턴스.
type Instance struct {
	ID    string // EC2 인스턴스 id (범례/표시용)
	Num   int    // 1-base, eb ssh -n 번호
	Label string // 표시용, 예: "api-beta-kr-j21#1"
}

// Source 는 로그 소스 드라이버(eb/ecs/...) 추상화.
type Source interface {
	// Environments 는 target(app+region)의 Ready 환경명을 발견한다.
	Environments(t Target) ([]string, error)
	// Instances 는 환경의 인스턴스 목록을 반환한다(시작 시 1회 스냅샷).
	Instances(t Target, env string) ([]Instance, error)
	// TailArgs 는 한 인스턴스를 스트리밍하는 eb argv(eb 제외) 를 반환한다.
	TailArgs(t Target, env string, inst Instance, follow bool, lines int) []string
}
```

- [ ] **Step 2: Write the failing test**

`internal/logs/eb_test.go`:

```go
package logs

import (
	"reflect"
	"testing"
)

func newFakeEB(out map[string][]byte) *EBSource {
	s := NewEBSource("/var/log/web.stdout.log")
	s.runner = func(name string, args ...string) ([]byte, error) {
		// 명령의 첫 서브커맨드로 응답을 고른다.
		key := args[0]
		return out[key], nil
	}
	return s
}

func TestTailArgsFollow(t *testing.T) {
	s := NewEBSource("/var/log/web.stdout.log")
	tgt := Target{Country: "kr", App: "kr-forceteller-api", Region: "ap-northeast-2"}
	got := s.TailArgs(tgt, "api-beta-kr-j21", Instance{Num: 2}, true, 100)
	want := []string{"ssh", "api-beta-kr-j21", "--region", "ap-northeast-2",
		"-n", "2", "-c", "sudo tail -n 100 -F /var/log/web.stdout.log"}
	if !reflect.DeepEqual(got, want) {
		t.Errorf("follow argv\n got %v\nwant %v", got, want)
	}
}

func TestTailArgsNoFollow(t *testing.T) {
	s := NewEBSource("/var/log/web.stdout.log")
	tgt := Target{Region: "ap-southeast-1"}
	got := s.TailArgs(tgt, "api-prod-en-j21", Instance{Num: 1}, false, 50)
	want := []string{"ssh", "api-prod-en-j21", "--region", "ap-southeast-1",
		"-n", "1", "-c", "sudo tail -n 50 /var/log/web.stdout.log"}
	if !reflect.DeepEqual(got, want) {
		t.Errorf("no-follow argv\n got %v\nwant %v", got, want)
	}
}

func TestEnvironmentsParsesText(t *testing.T) {
	s := newFakeEB(map[string][]byte{
		"elasticbeanstalk": []byte("api-beta-kr-j21\tapi-prod-kr-j21\n"),
	})
	envs, err := s.Environments(Target{App: "kr-forceteller-api", Region: "ap-northeast-2"})
	if err != nil {
		t.Fatal(err)
	}
	want := []string{"api-beta-kr-j21", "api-prod-kr-j21"}
	if !reflect.DeepEqual(envs, want) {
		t.Errorf("envs got %v want %v", envs, want)
	}
}

func TestInstancesParsesJSON(t *testing.T) {
	s := newFakeEB(map[string][]byte{
		"elasticbeanstalk": []byte(`{"EnvironmentResources":{"Instances":[{"Id":"i-aaa"},{"Id":"i-bbb"}]}}`),
	})
	got, err := s.Instances(Target{Region: "ap-northeast-2"}, "api-beta-kr-j21")
	if err != nil {
		t.Fatal(err)
	}
	if len(got) != 2 || got[0].ID != "i-aaa" || got[0].Num != 1 ||
		got[1].Num != 2 || got[1].Label != "api-beta-kr-j21#2" {
		t.Errorf("instances 파싱 틀림: %+v", got)
	}
}
```

> 주의: `Environments` 와 `Instances` 가 같은 첫 인자(`elasticbeanstalk`)를 쓰므로 위 두 테스트는 각각 자기 fake 를 만든다. (`TestEnvironmentsParsesText`/`TestInstancesParsesJSON` 은 별도 `newFakeEB` 인스턴스.)

- [ ] **Step 3: Run test to verify it fails**

Run: `go test ./internal/logs/ -v`
Expected: 컴파일 실패(`NewEBSource`/`EBSource` 미정의).

- [ ] **Step 4: eb.go 구현**

`internal/logs/eb.go`:

```go
package logs

import (
	"encoding/json"
	"fmt"
	"strconv"
	"strings"

	uqexec "github.com/un7qi3inc/un7qi3-cli/internal/exec"
)

// EBSource 는 eb ssh + aws elasticbeanstalk 로 동작하는 Source 구현.
type EBSource struct {
	path   string
	runner func(name string, args ...string) ([]byte, error)
}

// NewEBSource 는 기본 runner(uqexec.Run)를 쓰는 EB 드라이버를 만든다.
func NewEBSource(path string) *EBSource {
	return &EBSource{path: path, runner: uqexec.Run}
}

func (s *EBSource) Environments(t Target) ([]string, error) {
	out, err := s.runner("aws", "elasticbeanstalk", "describe-environments",
		"--application-name", t.App, "--region", t.Region,
		"--query", "Environments[?Status=='Ready'].EnvironmentName", "--output", "text")
	if err != nil {
		return nil, fmt.Errorf("환경 조회 실패(%s/%s): %w", t.App, t.Region, err)
	}
	return strings.Fields(string(out)), nil
}

func (s *EBSource) Instances(t Target, env string) ([]Instance, error) {
	out, err := s.runner("aws", "elasticbeanstalk", "describe-environment-resources",
		"--environment-name", env, "--region", t.Region, "--output", "json")
	if err != nil {
		return nil, fmt.Errorf("인스턴스 조회 실패(%s): %w", env, err)
	}
	var parsed struct {
		EnvironmentResources struct {
			Instances []struct {
				Id string `json:"Id"`
			} `json:"Instances"`
		} `json:"EnvironmentResources"`
	}
	if err := json.Unmarshal(out, &parsed); err != nil {
		return nil, fmt.Errorf("인스턴스 응답 파싱 실패: %w", err)
	}
	insts := make([]Instance, 0, len(parsed.EnvironmentResources.Instances))
	for i, in := range parsed.EnvironmentResources.Instances {
		insts = append(insts, Instance{
			ID:    in.Id,
			Num:   i + 1,
			Label: fmt.Sprintf("%s#%d", env, i+1),
		})
	}
	return insts, nil
}

func (s *EBSource) TailArgs(t Target, env string, inst Instance, follow bool, lines int) []string {
	tail := fmt.Sprintf("sudo tail -n %d", lines)
	if follow {
		tail += " -F"
	}
	tail += " " + s.path
	return []string{"ssh", env, "--region", t.Region, "-n", strconv.Itoa(inst.Num), "-c", tail}
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `go test ./internal/logs/ -v`
Expected: PASS (4 테스트)

- [ ] **Step 6: Commit**

```bash
git add internal/logs/source.go internal/logs/eb.go internal/logs/eb_test.go
git commit -m "feat(logs): Source 인터페이스 + EB 드라이버(발견/tail 명령) 추가"
```

---

## Task 3: 위치인자 → 국가/환경필터 해석 (순수 로직)

**Files:**
- Create: `internal/logs/selectlogic.go`
- Test: `internal/logs/selectlogic_test.go`

**Interfaces:**
- Consumes: `repocfg.LogsConfig`(국가 키 목록), `[]string`(발견된 환경명).
- Produces:
  - `func SplitArgs(filters []string, countryCodes []string) (country string, envFilters []string)` — 위치인자 중 국가코드와 일치하는 첫 토큰을 country 로, 나머지를 envFilters 로.
  - `func MatchEnvs(envs, filters []string) []string` — 모든 filter 를 (대소문자 무시) 부분문자열로 포함하는 환경만.

- [ ] **Step 1: Write the failing test**

`internal/logs/selectlogic_test.go`:

```go
package logs

import (
	"reflect"
	"testing"
)

func TestSplitArgsPicksCountry(t *testing.T) {
	c, f := SplitArgs([]string{"beta", "kr"}, []string{"kr", "en", "jp"})
	if c != "kr" {
		t.Errorf("country 기대 kr, 실제 %q", c)
	}
	if !reflect.DeepEqual(f, []string{"beta"}) {
		t.Errorf("envFilters 기대 [beta], 실제 %v", f)
	}
}

func TestSplitArgsNoCountry(t *testing.T) {
	c, f := SplitArgs([]string{"beta"}, []string{"kr", "en", "jp"})
	if c != "" {
		t.Errorf("country 없어야 함, 실제 %q", c)
	}
	if !reflect.DeepEqual(f, []string{"beta"}) {
		t.Errorf("envFilters 기대 [beta], 실제 %v", f)
	}
}

func TestMatchEnvsAndSubstring(t *testing.T) {
	envs := []string{"api-beta-kr-j21", "api-prod-kr-j21"}
	got := MatchEnvs(envs, []string{"BETA"})
	if !reflect.DeepEqual(got, []string{"api-beta-kr-j21"}) {
		t.Errorf("기대 [api-beta-kr-j21], 실제 %v", got)
	}
	if len(MatchEnvs(envs, []string{"beta", "prod"})) != 0 {
		t.Error("beta+prod 둘 다 포함하는 환경은 없어야 함")
	}
	if len(MatchEnvs(envs, nil)) != 2 {
		t.Error("필터 없으면 전부")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/logs/ -run "TestSplitArgs|TestMatchEnvs" -v`
Expected: 컴파일 실패(`SplitArgs`/`MatchEnvs` 미정의).

- [ ] **Step 3: selectlogic.go 구현**

`internal/logs/selectlogic.go`:

```go
package logs

import "strings"

// SplitArgs 는 위치인자를 (국가코드, 환경필터) 로 가른다. countryCodes 와
// 일치하는 첫 토큰을 country 로 취하고, 그 외는 모두 envFilters 로 남긴다.
func SplitArgs(filters []string, countryCodes []string) (country string, envFilters []string) {
	codes := make(map[string]bool, len(countryCodes))
	for _, c := range countryCodes {
		codes[c] = true
	}
	for _, f := range filters {
		if country == "" && codes[f] {
			country = f
			continue
		}
		envFilters = append(envFilters, f)
	}
	return country, envFilters
}

// MatchEnvs 는 모든 filter 를 (대소문자 무시) 부분문자열로 포함하는 환경만 반환.
func MatchEnvs(envs, filters []string) []string {
	var out []string
	for _, e := range envs {
		le := strings.ToLower(e)
		ok := true
		for _, f := range filters {
			if !strings.Contains(le, strings.ToLower(f)) {
				ok = false
				break
			}
		}
		if ok {
			out = append(out, e)
		}
	}
	return out
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/logs/ -run "TestSplitArgs|TestMatchEnvs" -v`
Expected: PASS (3 테스트)

- [ ] **Step 5: Commit**

```bash
git add internal/logs/selectlogic.go internal/logs/selectlogic_test.go
git commit -m "feat(logs): 위치인자→국가/환경필터 해석 로직 추가"
```

---

## Task 4: merged 출력 헬퍼 (범례·prefix·grep)

**Files:**
- Create: `internal/logs/mux.go`
- Test: `internal/logs/mux_test.go`

**Interfaces:**
- Consumes: `Instance`(Task 2), `internal/output`(색상).
- Produces:
  - `func RenderLegend(insts []Instance) string` — `#k → i-xxx` 매핑 텍스트.
  - `func PrefixLine(num int, line string) string` — `[#k] line` (색상은 output 패키지 사용).
  - `func GrepMatch(re *regexp.Regexp, line string) bool` — re==nil 이면 항상 true.

- [ ] **Step 1: Write the failing test**

`internal/logs/mux_test.go`:

```go
package logs

import (
	"regexp"
	"strings"
	"testing"
)

func TestRenderLegend(t *testing.T) {
	insts := []Instance{{ID: "i-aaa", Num: 1}, {ID: "i-bbb", Num: 2}}
	out := RenderLegend(insts)
	if !strings.Contains(out, "#1") || !strings.Contains(out, "i-aaa") ||
		!strings.Contains(out, "#2") || !strings.Contains(out, "i-bbb") {
		t.Errorf("범례에 #k/인스턴스 id 가 보여야 함:\n%s", out)
	}
}

func TestPrefixLine(t *testing.T) {
	got := PrefixLine(3, "hello")
	if !strings.Contains(got, "#3") || !strings.Contains(got, "hello") {
		t.Errorf("prefix 결과: %q", got)
	}
}

func TestGrepMatch(t *testing.T) {
	re := regexp.MustCompile("ERROR")
	if !GrepMatch(re, "x ERROR y") || GrepMatch(re, "ok") {
		t.Error("정규식 매칭 동작 틀림")
	}
	if !GrepMatch(nil, "anything") {
		t.Error("nil 정규식은 항상 통과")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/logs/ -run "TestRenderLegend|TestPrefixLine|TestGrepMatch" -v`
Expected: 컴파일 실패.

- [ ] **Step 3: mux.go 구현 (헬퍼)**

`internal/logs/mux.go`:

```go
package logs

import (
	"fmt"
	"regexp"
	"strings"

	"github.com/un7qi3inc/un7qi3-cli/internal/output"
)

// RenderLegend 은 시작 시 #k → 인스턴스 id 매핑을 표로 출력할 문자열을 만든다.
func RenderLegend(insts []Instance) string {
	var b strings.Builder
	for _, in := range insts {
		fmt.Fprintf(&b, "%s → %s\n", colorNum(in.Num, fmt.Sprintf("#%d", in.Num)), in.ID)
	}
	return b.String()
}

// PrefixLine 은 로그 라인에 색상 [#k] prefix 를 붙인다.
func PrefixLine(num int, line string) string {
	return colorNum(num, fmt.Sprintf("[#%d]", num)) + " " + line
}

// GrepMatch 은 re 가 nil 이면 항상 true, 아니면 line 매칭 여부.
func GrepMatch(re *regexp.Regexp, line string) bool {
	if re == nil {
		return true
	}
	return re.MatchString(line)
}

// colorNum 은 인스턴스 번호별로 색을 돌려 라벨에 입힌다.
func colorNum(num int, s string) string {
	palette := []func(string) string{
		output.Cyan, output.Green, output.Yellow, output.Magenta, output.Blue,
	}
	return palette[(num-1)%len(palette)](s)
}
```

> 검증 전 확인: `internal/output` 에 `Cyan/Green/Yellow/Magenta/Blue` 가 있는지 grep. 없는 색은 있는 것으로 교체(예: 모두 `Cyan/Green/Yellow` 3색 순환).

- [ ] **Step 4: output 색상 함수 존재 확인 후 보정**

Run: `grep -nE "func (Cyan|Green|Yellow|Magenta|Blue|Red|Dim)\(" internal/output/*.go`
없는 색이 있으면 `colorNum` 의 palette 를 존재하는 함수만으로 구성.

- [ ] **Step 5: Run test to verify it passes**

Run: `go test ./internal/logs/ -run "TestRenderLegend|TestPrefixLine|TestGrepMatch" -v`
Expected: PASS (3 테스트)

- [ ] **Step 6: Commit**

```bash
git add internal/logs/mux.go internal/logs/mux_test.go
git commit -m "feat(logs): merged 출력 헬퍼(범례·색상 prefix·grep) 추가"
```

---

## Task 5: 공유 패널 추출 (run/panel.go) + split.go 리팩터

**Files:**
- Create: `internal/run/panel.go`
- Modify: `internal/cmd/run/split.go`
- Test: 기존 `go test ./internal/cmd/run/ ./internal/run/` 회귀 없음

**Interfaces:**
- Produces:
  - `type Panel struct { Label string; Command string }` (in `internal/run`)
  - `func OpenPanel(mux Multiplexer, p Panel, dir SplitDir) error`
  - `type SplitDir` + 방향 상수(기존 split.go 의 `splitDir` 를 export 하거나 그대로 전달). **기존 `splitDir` 가 `internal/cmd/run` 에 있으므로**, 방향 파싱은 `internal/cmd/run` 에 남기고 `OpenPanel` 은 방향을 단순 enum(`run.SplitCol`/`run.SplitRow`)으로 받는다.

> 이 태스크는 동작 불변 리팩터다. 새 기능 없음 — 기존 테스트가 그대로 통과하면 성공.

- [ ] **Step 1: 기존 동작 기준선 확인**

Run: `go test ./internal/cmd/run/ ./internal/run/ -v`
Expected: 현재 PASS 상태 기록(기준선).

- [ ] **Step 2: run/panel.go 생성**

`internal/cmd/run/split.go` 의 `panel`, `shellLine`, `openPanel`, `openCmuxPanel`, `openITerm2Panel`, `openGhosttyPanel`, `openAppleTerminalPanel`, `parseCmuxSurface`, `shellQuote`, `appleScriptQuote` 를 `internal/run/panel.go` 로 옮긴다. 옮기면서:

- `panel` → 노출 타입 `Panel{ Label, Command string }` 로 단순화(cwd/env/cmd 조립은 호출부 책임).
- `openPanel(mux, pn, dir)` → `func OpenPanel(mux Multiplexer, p Panel, dir SplitDir) error`.
- 방향은 `type SplitDir int; const (SplitCol SplitDir = iota; SplitRow)` 로 정의.
- 각 opener 는 `p.Label`(패널 제목)·`p.Command`(셸 1줄)만 사용.

```go
// internal/run/panel.go
package run

// Panel 은 분할 패널 하나 — 제목과 실행할 셸 1줄.
type Panel struct {
	Label   string
	Command string
}

// SplitDir 은 분할 방향.
type SplitDir int

const (
	SplitCol SplitDir = iota // 좌우
	SplitRow                 // 상하
)

// OpenPanel 은 감지된 멀티플렉서로 패널을 연다.
func OpenPanel(mux Multiplexer, p Panel, dir SplitDir) error {
	switch mux {
	case MuxCmux:
		return openCmuxPanel(p, dir)
	case MuxITerm2:
		return openITerm2Panel(p, dir)
	case MuxGhostty:
		return openGhosttyPanel(p)
	case MuxAppleTerminal:
		return openAppleTerminalPanel(p)
	default:
		return errUnsupportedMux
	}
}
```

(나머지 opener/헬퍼는 기존 split.go 본문을 `p.Label`/`p.Command` 기준으로 이식. `errUnsupportedMux` 는 `errors.New("지원하지 않는 멀티플렉서")`.)

- [ ] **Step 3: split.go 가 run.Panel 을 쓰도록 수정**

`internal/cmd/run/split.go` 의 `buildPanels` 는 그대로 두되, 각 `panel` 을 `run.Panel{Label: name, Command: shellLine(...)}` 로 변환해 `run.OpenPanel(mux, rp, dir)` 호출. `splitDir` ↔ `run.SplitDir` 매핑 함수 추가. 이식된 함수들은 split.go 에서 제거.

- [ ] **Step 4: 빌드 + 회귀 테스트**

Run: `go build ./... && go test ./internal/cmd/run/ ./internal/run/ -v`
Expected: Step 1 과 동일하게 PASS (회귀 없음).

- [ ] **Step 5: dry-run 수동 확인 (동작 불변)**

Run: `make install && uq run forceteller-admin --country kr --split --dry-run`
Expected: Phase 3 와 동일한 분할 계획 출력(감지된 mux + 패널 목록).

- [ ] **Step 6: Commit**

```bash
git add internal/run/panel.go internal/cmd/run/split.go
git commit -m "refactor(run): 분할 패널 로직을 internal/run 공유로 추출 (동작 불변)"
```

---

## Task 6: logs split 출력 (cmux/iterm2 한정 + merged 폴백)

**Files:**
- Create: `internal/logs/split.go`
- Test: `internal/logs/split_test.go`

**Interfaces:**
- Consumes: `run.Multiplexer`, `run.Panel`, `run.OpenPanel`(Task 5), `Source.TailArgs`(Task 2).
- Produces:
  - `func SplitSupported(mux run.Multiplexer) bool` — cmux/iterm2 만 true.
  - `func BuildPanels(args [][]string, labels []string) []run.Panel` — eb argv 들을 `eb <argv...>` 셸 1줄 패널로.

- [ ] **Step 1: Write the failing test**

`internal/logs/split_test.go`:

```go
package logs

import (
	"strings"
	"testing"

	"github.com/un7qi3inc/un7qi3-cli/internal/run"
)

func TestSplitSupportedOnlyCmuxIterm2(t *testing.T) {
	if !SplitSupported(run.MuxCmux) || !SplitSupported(run.MuxITerm2) {
		t.Error("cmux/iterm2 는 지원이어야 함")
	}
	for _, m := range []run.Multiplexer{run.MuxGhostty, run.MuxAppleTerminal, run.MuxNone, run.MuxTmux} {
		if SplitSupported(m) {
			t.Errorf("%s 는 미지원이어야 함", m)
		}
	}
}

func TestBuildPanels(t *testing.T) {
	argv := [][]string{{"ssh", "api-beta-kr-j21", "-c", "sudo tail -F /var/log/web.stdout.log"}}
	panels := BuildPanels(argv, []string{"api-beta-kr-j21#1"})
	if len(panels) != 1 {
		t.Fatalf("패널 1개 기대, 실제 %d", len(panels))
	}
	if panels[0].Label != "api-beta-kr-j21#1" {
		t.Errorf("라벨 틀림: %q", panels[0].Label)
	}
	if !strings.HasPrefix(panels[0].Command, "eb ssh ") {
		t.Errorf("명령은 'eb ssh '로 시작해야 함: %q", panels[0].Command)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/logs/ -run "TestSplitSupported|TestBuildPanels" -v`
Expected: 컴파일 실패.

- [ ] **Step 3: split.go 구현**

`internal/logs/split.go`:

```go
package logs

import (
	"strings"

	"github.com/un7qi3inc/un7qi3-cli/internal/run"
)

// SplitSupported 는 logs 의 --split 이 지원하는 멀티플렉서인지(=cmux/iterm2).
func SplitSupported(mux run.Multiplexer) bool {
	return mux == run.MuxCmux || mux == run.MuxITerm2
}

// BuildPanels 는 eb argv(eb 제외) 목록을 "eb <argv...>" 셸 패널로 만든다.
func BuildPanels(argvs [][]string, labels []string) []run.Panel {
	panels := make([]run.Panel, 0, len(argvs))
	for i, argv := range argvs {
		panels = append(panels, run.Panel{
			Label:   labels[i],
			Command: "eb " + strings.Join(shellQuoteAll(argv), " "),
		})
	}
	return panels
}

// shellQuoteAll 은 각 인자를 작은따옴표로 감싼다(공백·특수문자 안전).
func shellQuoteAll(args []string) []string {
	out := make([]string, len(args))
	for i, a := range args {
		out[i] = "'" + strings.ReplaceAll(a, "'", `'\''`) + "'"
	}
	return out
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/logs/ -run "TestSplitSupported|TestBuildPanels" -v`
Expected: PASS (2 테스트)

- [ ] **Step 5: Commit**

```bash
git add internal/logs/split.go internal/logs/split_test.go
git commit -m "feat(logs): split 패널 빌더 + cmux/iterm2 지원 판정 추가"
```

---

## Task 7: logs 명령 와이어링 (오케스트레이션)

**Files:**
- Modify: `internal/cmd/logs/logs.go`
- Test: dry-run 동작은 검증 섹션에서 명령 실행으로 확인(단위테스트는 Task 1~6 으로 커버).

**Interfaces:**
- Consumes: `repocfg.Load/LogsFor`(T1), `logs.NewEBSource/Target/SplitArgs/MatchEnvs/RenderLegend/...`(T2~6), `run.DetectMultiplexer`, `authpkg.AwsStatus`, `huh`.
- Produces: 완성된 `uq logs` 명령.

- [ ] **Step 1: stub 제거 + 플래그·인자·상호배타 정의**

`internal/cmd/logs/logs.go` 의 `RunE` 와 플래그를 교체:

```go
Use:   "logs <repo> [필터...]",
Short: "EB 인스턴스 멀티플렉스 로그 스트리밍",
Long:  long,
Args:  cobra.MinimumNArgs(1),
RunE: func(cmd *cobra.Command, args []string) error {
	if split && noFollow {
		return fmt.Errorf("--split 과 --no-follow 는 함께 쓸 수 없습니다") // cobra → exit 2 경로
	}
	return runLogs(cmd, args[0], args[1:])
},
```

플래그(기존 `--instance`/`--since`/`--grep`/`--no-follow`/`--split` 중 `--since` 제거):

```go
cmd.Flags().IntVar(&instanceNum, "instance", 0, "1-base 인스턴스 번호로 한정 (0=전체)")
cmd.Flags().StringVar(&grep, "grep", "", "정규식으로 라인 필터")
cmd.Flags().BoolVar(&noFollow, "no-follow", false, "follow 없이 최근 N줄만 출력하고 종료")
cmd.Flags().BoolVar(&split, "split", false, "인스턴스별 패널 분리 (cmux/iterm2)")
cmd.Flags().BoolVar(&dryRun, "dry-run", false, "해석된 app/region/환경/명령만 출력")
cmd.MarkFlagsMutuallyExclusive("split", "no-follow")
```

- [ ] **Step 2: runLogs 오케스트레이션 구현**

같은 파일에 추가:

```go
func runLogs(cmd *cobra.Command, repo string, filters []string) error {
	w := cmd.OutOrStderr()

	// 1. allowlist 확인
	cfg, err := repocfg.Load()
	if err != nil {
		return err
	}
	lc, ok := cfg.LogsFor(repo)
	if !ok {
		fmt.Fprintln(w, output.Red("✗"), repo, "는 logs 미등록입니다. repos.yml 의 logs: 에 추가하세요.")
		os.Exit(1)
	}

	// 2. aws 인증 사전확인 (exit 4)
	if s := authpkg.AwsStatus(); !s.OK {
		return &authpkg.RequiredError{Msg: "aws 인증 안 됨. `uq auth login --aws-only` 실행"}
	}

	// 3. 국가 결정
	codes := countryCodes(lc)
	country, envFilters := logs.SplitArgs(filters, codes)
	isTTY := term.IsTerminal(int(os.Stdin.Fd()))
	if country == "" {
		if len(codes) == 1 {
			country = codes[0]
		} else if isTTY {
			country, err = pickCountry(codes)
			if err != nil {
				return err
			}
		} else {
			fmt.Fprintln(w, output.Red("✗"), "국가를 특정하세요(예:", strings.Join(codes, "/"), ")")
			os.Exit(1)
		}
	}
	tgt0, ok := lc.Countries[country]
	if !ok {
		fmt.Fprintln(w, output.Red("✗"), "알 수 없는 국가:", country)
		os.Exit(1)
	}
	tgt := logs.Target{Country: country, App: tgt0.App, Region: tgt0.Region}

	// 4. 환경 발견 + 필터 + 선택
	src := logs.NewEBSource(lc.PathOrDefault())
	envs, err := src.Environments(tgt)
	if err != nil {
		fmt.Fprintln(w, output.Red("✗"), err)
		os.Exit(1)
	}
	matched := logs.MatchEnvs(envs, envFilters)
	env, err := resolveEnv(matched, isTTY)
	if err != nil {
		fmt.Fprintln(w, output.Red("✗"), err)
		os.Exit(1)
	}

	// 5. 인스턴스 발견 (스냅샷)
	insts, err := src.Instances(tgt, env)
	if err != nil {
		fmt.Fprintln(w, output.Red("✗"), err)
		os.Exit(1)
	}
	if instanceNum > 0 {
		insts = filterInstance(insts, instanceNum)
	}
	if len(insts) == 0 {
		fmt.Fprintln(w, output.Red("✗"), "대상 인스턴스가 없습니다")
		os.Exit(1)
	}

	// 6. dry-run / split / merged
	lines := 100
	if dryRun {
		return printLogsPlan(w, tgt, env, insts, src, !noFollow, lines)
	}
	if split {
		mux := run.DetectMultiplexer()
		if logs.SplitSupported(mux) {
			return runLogsSplit(w, src, tgt, env, insts, mux, !noFollow, lines)
		}
		fmt.Fprintln(w, output.Yellow("⚠"), "현재 터미널은 분할 미지원 — merged 로 진행")
	}
	return logs.StreamMerged(cmd.Context(), w, src, tgt, env, insts, !noFollow, lines, grep)
}
```

> 구현 시 헬퍼 `countryCodes`, `pickCountry`(huh.Select), `resolveEnv`(0개→에러, 1개→자동, 다수→huh 또는 비TTY 에러), `filterInstance`, `printLogsPlan`, `runLogsSplit` 를 같은 파일/`internal/logs` 에 둔다. `StreamMerged` 는 Step 3 에서 구현.

- [ ] **Step 3: StreamMerged 스트리밍 구현 (internal/logs/mux.go 보강)**

`internal/logs/mux.go` 에 추가 — 각 인스턴스의 `eb` 프로세스를 spawn 해 라인을 prefix·grep 후 합치고, 인스턴스별 실패는 격리:

```go
func StreamMerged(ctx context.Context, w io.Writer, src Source, t Target, env string,
	insts []Instance, follow bool, lines int, grep string) error {

	var re *regexp.Regexp
	if grep != "" {
		var err error
		if re, err = regexp.Compile(grep); err != nil {
			return fmt.Errorf("--grep 정규식 오류: %w", err)
		}
	}
	fmt.Fprint(w, RenderLegend(insts)) // 범례

	var wg sync.WaitGroup
	var mu sync.Mutex // w 직렬화
	for _, in := range insts {
		wg.Add(1)
		go func(in Instance) {
			defer wg.Done()
			args := src.TailArgs(t, env, in, follow, lines)
			cmd := exec.CommandContext(ctx, "eb", args...)
			stdout, err := cmd.StdoutPipe()
			cmd.Stderr = cmd.Stdout // eb 경고도 같이
			if err == nil {
				err = cmd.Start()
			}
			if err != nil {
				mu.Lock()
				fmt.Fprintln(w, PrefixLine(in.Num, output.Red("접속 실패: ")+err.Error()))
				mu.Unlock()
				return
			}
			sc := bufio.NewScanner(stdout)
			sc.Buffer(make([]byte, 0, 64*1024), 1024*1024)
			for sc.Scan() {
				line := sc.Text()
				if !GrepMatch(re, line) {
					continue
				}
				mu.Lock()
				fmt.Fprintln(w, PrefixLine(in.Num, line))
				mu.Unlock()
			}
			if err := cmd.Wait(); err != nil {
				mu.Lock()
				fmt.Fprintln(w, PrefixLine(in.Num, output.Dim("스트림 종료: "+err.Error())))
				mu.Unlock()
			}
		}(in)
	}
	wg.Wait()
	return nil
}
```

(import: `bufio`, `context`, `io`, `os/exec`, `regexp`, `sync`, `output`.)

- [ ] **Step 4: 빌드 + 기존 단위테스트 회귀 확인**

Run: `go build ./... && go test ./internal/logs/... ./internal/repocfg/... ./internal/run/... ./internal/cmd/run/...`
Expected: 전부 PASS.

- [ ] **Step 5: dry-run 동작 확인 (aws 없이도 발견 단계까지 가므로, 발견 실패는 exit 1 로 정상)**

```bash
make install
uq logs                         # exit 2 (인자 필요)
uq logs forceteller-app         # exit 1, "logs 미등록"
uq logs forceteller-api --help  # 플래그/예시 노출, stub 문구 없음
```
Expected: 각 메시지/exit 코드 일치.

- [ ] **Step 6: Commit**

```bash
git add internal/cmd/logs/logs.go internal/logs/mux.go
git commit -m "feat(logs): uq logs 오케스트레이션(국가/환경 선택·merged/split·dry-run) 구현"
```

---

## Task 8: doctor 에 eb CLI 추적 추가

**Files:**
- Modify: `internal/cmd/doctor/doctor.go`

**Interfaces:**
- Consumes: 기존 도구 테이블 패턴(`Name/Roles/Fix/Run`, `versionCheck`).

- [ ] **Step 1: aws 항목 뒤에 eb 항목 추가**

`internal/cmd/doctor/doctor.go` 의 `aws` 도구 블록 바로 뒤에:

```go
{
	Name:  "eb",
	Roles: []string{RoleInfra},
	Fix:   "pip install awsebcli",
	Run:   versionCheck("eb", []string{"--version"}, `EB CLI (\S+)`),
},
```

- [ ] **Step 2: 빌드 + doctor 출력 확인**

Run: `make install && uq doctor 2>&1 | grep -i "eb"`
Expected: 인프라 그룹에 `eb` 항목 표시(설치돼 있으면 버전, 아니면 `pip install awsebcli` 안내).

- [ ] **Step 3: Commit**

```bash
git add internal/cmd/doctor/doctor.go
git commit -m "feat(doctor): eb CLI 설치/버전 추적 추가"
```

---

## Self-Review 기록

- **Spec coverage**: allowlist(T1)·발견/tail(T2)·국가환경선택(T3)·merged 식별(T4·T7-S3)·split cmux/iterm2(T5,T6,T7)·실패격리(T7-S3)·dry-run(T7)·aws 사전확인(T7)·doctor eb(T8). `--since`·CloudWatch·세션중 재발견은 스펙의 "제외"와 일치.
- **Placeholder scan**: 각 코드 스텝에 실제 Go 코드 포함. 단, `internal/output` 색상 함수 존재는 T4-S4 에서 확인 후 보정(미존재 시 대체) — 명시적 확인 스텝으로 처리.
- **Type consistency**: `Target/Instance/Source`(T2) → T3·T4·T6·T7 에서 동일 시그니처 사용. `run.Panel/OpenPanel/SplitDir/Multiplexer 상수`(T5) → T6·T7 에서 사용. `repocfg.LogsConfig/CountryTarget/LogsFor/PathOrDefault`(T1) → T7 에서 사용.
- **확인 필요(구현 중)**: ① `internal/output` 색상 함수 목록(T4-S4) ② `eb ssh -c` 가 `tail -F` stdout 을 라인 단위로 흘리는지(스트리밍 실측은 실환경 필요 — 단위테스트 대상 아님) ③ `eb --version` 의 정확한 출력 포맷(정규식 `EB CLI (\S+)` 보정 가능).
